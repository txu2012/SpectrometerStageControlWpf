<Window x:Class="SpectrometerStageControlWpf.SurfacePlotterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:SurfacePlot="clr-namespace:OpenControls.Wpf.SurfacePlot;assembly=OpenControls.Wpf.SurfacePlot"
        xmlns:SurfacePlotView="clr-namespace:OpenControls.Wpf.SurfacePlot.View;assembly=OpenControls.Wpf.SurfacePlot"
        xmlns:local="clr-namespace:SpectrometerStageControlWpf"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        ResizeMode="CanResize"
        KeyDown="Window_KeyDown"
        Title="SurfacePlotterView" Height="720" Width="1280">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/OpenControls.Wpf.Utilities;component/View/Dictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Background="Gainsboro">
            <Border x:Name="bdTester" Grid.Row="0" Grid.Column="0" Margin="0" Background="LightSteelBlue">
                <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="99" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="25"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" Grid.Column="0" x:Name="btnStart" Content="Start" HorizontalAlignment="Left" Width="80" Visibility="{Binding IsRunning, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                    <Button Grid.Row="0" Grid.Column="0" x:Name="btnStop" Content="Stop" HorizontalAlignment="Left" Width="80" Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <Label Grid.Row="0" Grid.Column="2" x:Name="lblSpeed" Content="Speed:" Style="{StaticResource LabelStyle}"/>
                    <ComboBox Grid.Row="0" Grid.Column="4" x:Name="cbSpeed" ItemsSource="{Binding Speeds}" SelectedValue="{Binding SelectedSpeed}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBooleanConverter}}"/>
                    <Button Grid.Row="2" Grid.Column="0" x:Name="btnUpdate" Content="Update" HorizontalAlignment="Left" Width="80" />
                    <RadioButton Grid.Row="4" Grid.Column="0" x:Name="rbTestData1" Content="TestData 1" HorizontalAlignment="Center" VerticalAlignment="Center" IsChecked="True"/>
                    <RadioButton Grid.Row="4" Grid.Column="2" x:Name="rbTestData2" Content="TestData 2" HorizontalAlignment="Center" VerticalAlignment="Center" IsChecked="False"/>
                </Grid>
            </Border>
        </Border>
        <SurfacePlotView:ConfigurationControl Grid.Row="2" Grid.Column="0"  x:Name="_configurationControl" HorizontalAlignment="Stretch" Background="Gainsboro" BorderThickness="0"/>
        <WindowsFormsHost Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Margin="0" Background="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <SurfacePlot:SurfacePlotControl x:Name="_surfacePlotControl"/>
        </WindowsFormsHost>
    </Grid>
</Window>
